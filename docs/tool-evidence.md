# Tool Evidence

All source references are from Jenkins tag `jenkins-2.579` at commit `9095ea3a5c5e7dcd392695a5dd880af1c9910ddf`.

| Tool | Endpoint used | Evidence | Permission evidence / behavior |
| --- | --- | --- | --- |
| `jenkins_whoami` | `GET whoAmI/api/json` | `WhoAmI` exposes `/whoAmI` and `getApi()` at `hudson/security/WhoAmI.java:22-39`. | Unprotected root action, reports current authentication. |
| `jenkins_version` | `GET api/json`, read `X-Jenkins` header | `Api.setHeaders` sets `X-Jenkins` at `hudson/model/Api.java:307-314`; official docs say any `.../api/*` page exposes version header. | Same permission as top API. |
| `jenkins_health` | `GET api/json` | `Jenkins.getApi()` at `jenkins/model/Jenkins.java:1368-1378`; Remote Access API docs confirm top-level API. | Jenkins filters exported data by permissions. |
| `jenkins_get_json` | `GET <relative>/api/json` | Remote Access API docs say API is under `.../api/`; source uses `Api` across Jenkins model objects. | GET only; external URLs/traversal rejected locally. |
| `jenkins_list_jobs` | `GET api/json?tree=jobs[...]` | Jenkins exports `jobs` at `Jenkins.java:1769-1772`. | Only jobs visible to user are returned. |
| `jenkins_get_job` | `GET job/{name}/api/json` | `AbstractItem.getApi()` at `hudson/model/AbstractItem.java:604-609`; official docs show repeated `/job/...` path format. | Jenkins item permissions apply. |
| `jenkins_get_job_config` | `GET job/{name}/config.xml` | `AbstractItem.doConfigDotXml` maps `config.xml` at `AbstractItem.java:837-877`; `writeConfigDotXml` requires `EXTENDED_READ`, serializes the in-memory item, and redacts without `CONFIGURE` at `AbstractItem.java:880-905`. | Requires Jenkins `EXTENDED_READ`; secrets may be redacted. XML formatting, comments, and ordering are not guaranteed to round-trip byte-for-byte. |
| `jenkins_list_builds` | `GET job/{name}/api/json?tree=builds[...]` | `Job.getNewBuilds()` exports `builds` at `hudson/model/Job.java:787-793`. | Job read permissions apply. |
| `jenkins_get_build` | `GET job/{name}/{build}/api/json` | `Run.getApi()` at `hudson/model/Run.java:1530-1532`. | Build/job read permissions apply. |
| `jenkins_get_build_log` | `GET job/{name}/{build}/consoleText` | `Run.doConsoleText` serves raw console text at `Run.java:2217-2245`; `Run.getLogText()` and `AnnotatedLargeText.doProgressiveText` evidence progressive support at `Run.java:1510-1515` and `AnnotatedLargeText.java:127-141`. | Build/job read permissions apply; output is truncated locally. Jenkins 2.569 changed HTML/progressive partial-line handling, not this raw `consoleText` route. |
| `jenkins_get_build_log_chunk` | `GET job/{name}/{build}/logText/progressiveText?start={cursor}` | `Run.getLogText()` exposes `AnnotatedLargeText` at `Run.java:1510-1515`; `AnnotatedLargeText.doProgressiveText` delegates at `AnnotatedLargeText.java:127-141`. Jenkins' pinned Stapler `LargeText.doProgressTextImpl` reads `start`, emits `X-Text-Size`, and emits `X-More-Data` for incomplete logs at `LargeText.java:433-485` in Stapler `2107.v8dfcb_e8ed317`. | Build/job read permissions apply. The tool advances only to Jenkins' returned cursor after the complete bounded response is received. |
| `jenkins_search_build_log` | `GET job/{name}/{build}/consoleText` | `Run.doConsoleText` serves raw console text at `Run.java:2217-2245`. | Build/job read permissions apply. Search is an exact case-sensitive literal, streams locally, and stops at configured scan/match bounds. |
| `jenkins_get_build_artifacts` | `GET job/{name}/{build}/api/json?tree=artifacts[...]` | `Run.getArtifacts()` is exported at `Run.java:1075-1080`; artifact browser at `Run.java:2183-2191`. | `ARTIFACTS` permission may apply when enabled. |
| `jenkins_start_artifact_download` | `GET job/{name}/{build}/api/json`, `GET job/{name}/{build}/artifact/{relativePath}` | Build number is resolved through `Run.getApi()` at `Run.java:1530-1532`; `Run.doArtifact()` returns `DirectoryBrowserSupport` rooted at the artifact manager at `Run.java:2183-2191`, whose child-path resolution is at `DirectoryBrowserSupport.java:205-226`. | Requires the local artifact download gate plus Jenkins build/job read permission. Jenkins `Run.ARTIFACTS` applies when enhanced artifact permission is enabled. External artifact-manager redirects are rejected rather than followed. |
| `jenkins_get_artifact_download_status` | Local progress file read | Reads `.progress.json` written under `JENKINS_MCP_ARTIFACT_DOWNLOAD_DIR`. | Requires local artifact download config. No Jenkins request. |
| `jenkins_cancel_artifact_download` | Local cancel marker write | Writes `.cancel` only while local progress says the operation is running; the background artifact worker checks it while streaming. | Requires local artifact download config. No Jenkins request. Terminal operations return `cancel_requested=false`; cancellation does not rewrite progress. |
| `jenkins_get_test_report` | `GET job/{name}/{build}/testReport/api/json` | Core references test report actions at `AbstractBuild.java:1096-1115`; official JUnit plugin docs state test report functionality was split from core, and Jenkins 2.574 stopped bundling JUnit. | Plugin-dependent; 404 is returned clearly when no test action/plugin exists. |
| `jenkins_list_queue` | `GET queue/api/json` | `Queue.getApi()` at `hudson/model/Queue.java:1955-1957`; `Queue.getItems()` exported at `Queue.java:787-810`. | Jenkins queue visibility/permissions apply. |
| `jenkins_get_queue_item` | `GET queue/item/{id}/api/json` | `Queue.Item.getUrl()` returns `queue/item/{id}/` at `Queue.java:2444-2452`; Jenkins test uses `queue/item/{id}/api/xml`. | Item read/discover permissions apply. |
| `jenkins_list_views` | `GET api/json?tree=views[...]` | Jenkins exports views at `Jenkins.java:1870-1876`. | Jenkins view permissions apply. |
| `jenkins_get_view` | `GET view/{name}/api/json` | `View.getApi()` at `hudson/model/View.java:623-625`; `View.getItems()` exports view jobs at `View.java:191-196`. | Jenkins view/item permissions apply. |
| `jenkins_list_nodes` | `GET computer/api/json` | `Jenkins.getComputer()` binds `/computer/` at `Jenkins.java:1478-1485`; `ComputerSet.getApi()` at `ComputerSet.java:470-472`. | Jenkins computer permissions apply. |
| `jenkins_get_node` | `GET computer/{name}/api/json` | `Jenkins.getComputer(name)` resolves names at `Jenkins.java:1986-1994`; `Computer.getApi()` at `Computer.java:1423-1425`. | Jenkins computer permissions apply. |
| `jenkins_list_plugins` | `GET pluginManager/api/json` | `Jenkins.getPluginManager()` at `Jenkins.java:1330-1332`; `PluginManager.getApi()` checks `SYSTEM_READ` at `PluginManager.java:416-418`; plugins exported at `PluginManager.java:1268-1273`. | Requires `SYSTEM_READ`; non-admin users may get 403. |
| `jenkins_start_workspace_bundle_download` | `GET job/{name}/api/json`, `GET queue/api/json`, optional `GET job/{name}/{build}/api/json`, `GET job/{name}/ws/**/*zip*/{safe-job}{anchor}.zip`, `GET job/{name}/{anchor}/consoleText` | Job queue state and `lastBuild` are exported at `Job.java:294-304` and `Job.java:960-971`; run `queueId`, `result`, `building`, and `inProgress` are exported at `Run.java:448-523`; queue item identity/task/state are exported at `Queue.java:2260-2334` and `Queue.java:2443-2463`. `AbstractProject.doWs` enforces `Item.WORKSPACE` at `AbstractProject.java:1904-1927`, while `getSomeWorkspace` explicitly makes only a cursory effort to find some build workspace at `AbstractProject.java:532-565`. `DirectoryBrowserSupport` writes zip archives at `DirectoryBrowserSupport.java:262-275`; exact console text is served at `Run.java:2217-2245`. | Requires the local workspace download gate plus Jenkins job/build read and `Item.WORKSPACE`. The worker waits for no queue/run activity, anchors to stable `lastBuild`, checks REST state before/during/after `/ws`, and retries once after change. This is best-effort because `/ws` exposes no build identity; an explicit non-current build is rejected. Matching local callers join one detached operation, and `force_refresh` bypasses completed-capture reuse. |
| `jenkins_start_workspace_path_download` | `GET job/{name}/api/json`, `GET queue/api/json`, optional `GET job/{name}/{build}/api/json`, `GET job/{name}/ws/{path}`, folder `GET job/{name}/ws/{path}/**/*zip*/{safe-name}.zip`, `GET job/{name}/{anchor}/consoleText` | Uses the same exported job/queue/run guard evidence as the bundle tool. Workspace child paths are served by `AbstractProject.doWs` at `AbstractProject.java:1904-1927`; `DirectoryBrowserSupport` resolves child paths and recognizes `*zip*` at `DirectoryBrowserSupport.java:205-226`, then writes directory archives at `DirectoryBrowserSupport.java:262-275`. | Same guard, permission, best-effort identity, join/reuse, and historical-build restrictions as the bundle tool. Requested paths reject external URLs, absolute paths, traversal, Jenkins magic path segments, and wildcards. Folder archives are extracted and deleted; files stream directly. |
| `jenkins_get_workspace_bundle_status` | Local SQLite registry and progress-file read | Reads operation ownership/status from `.operations/workspace-operations.sqlite3` and atomically written `progress.json` under `JENKINS_MCP_WORKSPACE_DOWNLOAD_DIR`. | Requires local workspace download config. No Jenkins request. Reports wait/capture phase, transfer progress, worker heartbeat, disposition-related paths, and stale-worker failure. |
| `jenkins_cancel_workspace_bundle_download` | Local SQLite update and cancel marker write | Durably sets `cancel_requested` only for a running shared-registry row and attempts to write its local cancel marker. | Requires local workspace download config. No Jenkins request. Detached workers check cancellation while waiting, streaming, and extracting. Terminal operations are unchanged; a marker-write failure is reported while a running registry request remains set. |
| `jenkins_cleanup_workspace_bundle_operations` | Local registry/progress/output deletion | Reads the SQLite registry plus legacy local indexes, marks stale workers failed, and removes only explicitly bounded terminal operations older than the requested age. | Requires local workspace download config and explicit invocation. No Jenkins request. Active, recent, and invalid operations are skipped; stale output is removed. |
| `jenkins_trigger_build` | `POST job/{name}/build` | Official docs say POST `JENKINS_URL/job/JOBNAME/build`; source `doBuild` at `ParameterizedJobMixIn.java:205-237`. | Requires local write gate and Jenkins build permission. |
| `jenkins_trigger_build_with_parameters` | `POST job/{name}/buildWithParameters` | Official docs show `buildWithParameters`; source at `ParameterizedJobMixIn.java:243-254`. | Requires local write gate and Jenkins build permission. |
| `jenkins_stop_build` | `POST job/{name}/{build}/stop` | Jenkins core evidence is `AbstractBuild.doStop()`, `@RequirePOST` at `AbstractBuild.java:1405-1415`. | Requires local write gate and Jenkins build stop/cancel permission. Plugin-defined run types such as Pipeline may expose different stop behavior; their support is not proven by this core endpoint. |
| `jenkins_cancel_queue_item` | `POST queue/cancelItem?id={id}` | `Queue.doCancelItem` is `@RequirePOST` and applies read/cancel checks at `Queue.java:756-774`. The Jenkins 2.568 security change affected deprecated item-local `doCancelQueue`, not this route. | Requires local write gate plus Jenkins item read and cancel permission. |
| `jenkins_enable_job` | `POST job/{name}/enable` | `ParameterizedJob.doEnable` is `@RequirePOST` at `ParameterizedJobMixIn.java:547-552`. | Requires local write gate and Jenkins `CONFIGURE`. |
| `jenkins_disable_job` | `POST job/{name}/disable` | `ParameterizedJob.doDisable` is `@RequirePOST` at `ParameterizedJobMixIn.java:539-545`. | Requires local write gate and Jenkins `CONFIGURE`. |
| `jenkins_create_job` | `POST createItem?name=...` | `Jenkins.doCreateItem` is `@RequirePOST` at `Jenkins.java:4229-4232`; XML creation path is at `ItemGroupMixIn.java:147-192`. | Optional; requires write + job-config gate and Jenkins `CREATE`. |
| `jenkins_copy_job` | `POST createItem?mode=copy&from=...&name=...` | Copy mode in `ItemGroupMixIn.java:173-188`; permission checks in `ItemGroupMixIn.java:231-255`. | Optional; requires write + job-config gate and Jenkins permissions. |
| `jenkins_update_job_config` | `POST job/{name}/config.xml` | POST branch is at `AbstractItem.java:862-873`; `updateByXml` validates/deserializes then reserializes at `AbstractItem.java:918-951`. | Optional; requires write + job-config gate and Jenkins `CONFIGURE`. Submitted XML is not preserved byte-for-byte; plaintext secrets are encrypted during reserialization. |
| `jenkins_delete_job` | `POST job/{name}/doDelete` | `AbstractItem.doDoDelete` is `@RequirePOST` at `AbstractItem.java:693-724`. | Optional; requires write + job-config + delete gates and Jenkins `DELETE`. |

## Explicitly Not Implemented

Script console, restart, safe restart, quiet down, plugin install/update, credential read/write, node creation/deletion, global config changes, and user management are intentionally absent.
